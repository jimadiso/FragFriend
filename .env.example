# PostgreSQL connection settings
DB_USER=postgres
DB_PASSWORD=replace_me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fragfriend

# Replace with a long, random value in your local .env file.
JWT_SECRET=replace_with_a_long_random_value

